
INSERT INTO MenuItems (item_id, item_name, price, category)
VALUES 
(1, 'Pizza', 250, 'Fast Food'),
(2, 'Burger', 150, 'Snacks'),
(3, 'Pasta', 200, 'Italian');

UPDATE MenuItems
SET price = 275
WHERE item_id = 1;

DELETE FROM MenuItems
WHERE item_id = 2;

SELECT AVG(price) AS average_price
FROM MenuItems;

SELECT SUM(price) AS total_price
FROM MenuItems;
